# Learn Rest API written in Go lang

### How to Setup

1. Install Go lang
2. run `bash go get` in terminal
3. run `bash go mod tidy` in terminal
4. run `bash go run main.go` in terminal

### You can config anything in main.go
